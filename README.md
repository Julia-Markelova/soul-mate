# База данных

- Запуск и добавление таблиц:
```
   $ cd database
   $ export `cat .env | xargs`  // там порт для бд указан
   $ docker-compose up
```

- Подключение:
  - database: `soul_mate`
  - user: `postgres`
  - port: `55435`
  - password: `1234`
  
